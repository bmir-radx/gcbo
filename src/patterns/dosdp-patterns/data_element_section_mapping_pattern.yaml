pattern_name: data_element_section_mapping

pattern_iri: https://bmir-radx.github.io/patterns/data_element_section_mapping.yaml

description: 'Pattern mapping data elements to related sections'

classes:
  thing: owl:Thing

annotationProperties:
  is_in_section: BMIR:isInSection

vars:
  section: thing

annotations:
- annotationProperty: is_in_section
  var: section
